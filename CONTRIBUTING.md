# Руководство по разработке

Спасибо за интерес к проекту! Этот документ описывает стандарты разработки и процесс внесения изменений.

## 🎯 Стандарты кода

### Именование файлов

- **Компоненты и классы**: PascalCase (например, `LoginScreen.ts`, `BaseBrowserElement.ts`)
- **Утилиты и helpers**: camelCase (например, `apiClient.ts`, `dataBuilder.ts`)
- **Конфигурационные файлы**: kebab-case (например, `playwright.config.base.ts`)

### Именование классов и интерфейсов

- Классы: PascalCase (например, `LoginScreen`, `AuthFlow`)
- Интерфейсы: PascalCase с префиксом `I` (например, `IEnvConfig`)
- Типы: PascalCase (например, `Environment`, `TestContext`)

### Структура импортов

Импорты должны быть отсортированы в следующем порядке:
1. Внешние библиотеки (например, `@playwright/test`)
2. Внутренние модули из `src/`
3. Относительные импорты (`../`, `./`)

Используйте barrel exports где возможно:

```typescript
// ✅ Хорошо
import { Button, Input } from '../../core/browser-elements';

// ❌ Плохо
import { Button } from '../../core/browser-elements/Button';
import { Input } from '../../core/browser-elements/Input';
```

## 🏗️ Архитектурные правила

### Components (строго)

- **Единственное место** для `page.locator(...)`
- Все UI взаимодействия (click, fill, select)
- UI assertions
- Должны наследоваться от `BaseBrowserElement`

### Screens

- Композиция компонентов
- Экрано-уровневые действия
- **НЕ** содержат прямых вызовов `page.locator`
- Именование: `{Feature}Screen.ts` (PascalCase)

### App

- Навигация, refresh, tabs
- Доступ к screens через `app.screens`
- **НЕ** содержит feature-specific логику

### Flows

- Бизнес-логика и user journeys
- Оркестрация app + screens + components
- **НЕ** содержат locators

### Core

- `core/browser-elements` - переиспользуемые UI элементы
- `core/utils` - переиспользуемые утилиты
- `core/validators` - переиспользуемые валидаторы

## 🧪 Написание тестов

### Структура теста

Каждый тест должен следовать UI-only подходу:

```typescript
test('should perform action', async ({ app }) => {
  // 1. UI Execution (Act)
  const flow = new FeatureFlow(app);
  await flow.performAction('test@example.com', 'password');
  
  // 2. Assertions
  await expect(app.page).toHaveURL(/expected-url/);
});
```

### Правила

- Каждый тест должен быть изолированным и запускаться параллельно
- Используйте чистый UI подход без API setup
- Используйте декоратор `@step()` для всех действий
- Названия тестов должны быть описательными

## 📝 Коммиты

### Формат сообщений коммитов

Используйте conventional commits:

- `feat: добавить новую функциональность`
- `fix: исправить баг`
- `refactor: рефакторинг кода`
- `docs: обновить документацию`
- `test: добавить/обновить тесты`
- `chore: обновить зависимости, конфигурацию`

### Примеры

```
feat: добавить новый экран Dashboard
fix: исправить селектор для кнопки входа
refactor: переименовать main-screen в MainScreen
docs: обновить README со структурой core/
```

## 🔍 Code Review

### Перед отправкой PR

- [ ] Код соответствует стандартам проекта
- [ ] Все тесты проходят
- [ ] Нет ошибок линтера
- [ ] Код отформатирован (Prettier)
- [ ] Обновлена документация если нужно
- [ ] Коммиты следуют conventional commits

### Проверка кода

Перед коммитом выполните:

```bash
# Проверка линтера
npm run lint

# Форматирование кода
npm run format

# Проверка типов
npx tsc --noEmit

# Запуск тестов
npm test
```

## 🛠️ Инструменты разработки

### Pre-commit hooks

Проект использует husky и lint-staged для автоматической проверки кода перед коммитом.

### Форматирование

Используйте Prettier для форматирования:

```bash
npm run format
```

### Линтинг

Используйте ESLint для проверки кода:

```bash
npm run lint
```

## 📦 Добавление новых зависимостей

Перед добавлением новой зависимости:

1. Обсудите необходимость с командой
2. Убедитесь, что зависимость не конфликтует с существующими
3. Добавьте в соответствующий раздел `package.json`:
   - `dependencies` - для runtime зависимостей
   - `devDependencies` - для development зависимостей
4. Обновите документацию если нужно

## 🐛 Сообщение о багах

При сообщении о баге укажите:

1. Описание проблемы
2. Шаги для воспроизведения
3. Ожидаемое поведение
4. Фактическое поведение
5. Окружение (OS, Node.js версия, браузер)
6. Скриншоты/логи если применимо

## ❓ Вопросы

Если у вас есть вопросы, создайте issue с тегом `question`.

Спасибо за вклад в проект! 🎉

