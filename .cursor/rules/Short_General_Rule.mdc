---
alwaysApply: true
---

# 🚨 STRICT ARCHITECTURE ENFORCEMENT — ZERO DEVIATIONS

You are an **expert TypeScript / Playwright / Frontend Architect**.

Your primary responsibility is to **STRICTLY follow the existing project structure provided by the user (including screenshots)**.

❗ **Architecture correctness is more important than functionality.**
❗ **Any deviation is considered a failure.**

---

## 🧱 SOURCE OF TRUTH

The **ONLY valid project structure** is the one shown in the **user-provided screenshot**.

You MUST:
- Follow existing folders exactly
- Follow existing naming conventions
- Place files ONLY where the screenshot indicates

🚫 You are NOT allowed to:
- Invent new folders
- Restructure the project
- “Improve” or “simplify” the architecture
- Move logic between layers

If something is unclear → **STOP AND ASK THE USER**.

---

## 🧭 MANDATORY LAYERING (PHYSICAL, NOT CONCEPTUAL)

The architecture is **PHYSICALLY ENFORCED**:

app
└─ screens
└─ components


This hierarchy is **ABSOLUTE**.

---

## 1️⃣ `components/` — UI IMPLEMENTATION LAYER (ONLY PLACE FOR UI)

**ONLY components are allowed to:**
- Contain selectors / locators
- Access DOM
- Call Playwright UI APIs:
  - `click`
  - `fill`
  - `type`
  - `hover`
  - `expect`
  - `page.locator`
  - `page.getByTestId`

Components represent:
- Single UI elements
- Small reusable UI blocks

🚫 Components MUST NOT:
- Contain business logic
- Handle navigation
- Orchestrate flows
- Know about tests or scenarios

➡️ **ALL UI INTERACTION LIVES HERE — NOWHERE ELSE**

---

## 2️⃣ `screens/` — PAGE COMPOSITION ONLY

Screens are **composition layers**.

Screens:
- Instantiate and compose components
- Expose page-level actions using component methods
- Represent full pages or major views

🚫 Screens MUST NEVER:
- Contain locators
- Call `page.locator`, `getByText`, `getByRole`, etc.
- Interact with DOM directly
- Call Playwright UI methods

✅ Screens may ONLY call **public component methods**.

---

## 3️⃣ `app/` — INFRASTRUCTURE / FRAMEWORK LAYER

The `app` layer is **LOW-LEVEL ONLY**.

Allowed responsibilities:
- Browser / context / page lifecycle
- Navigation helpers
- Tabs / windows handling
- Waiting utilities
- Framework-level abstractions

🚫 `app` MUST NOT:
- Know about features
- Know about screens or components
- Contain selectors
- Contain UI logic

---

## 🧨 ABSOLUTE RULE (NO EXCEPTIONS)

> **If code interacts with the UI, it MUST live in `components/`.**

Violations include:
- Locators outside `components`
- `click()`, `fill()`, `expect()` outside `components`
- DOM access outside `components`

➡️ **This is an architectural violation.**

---

## 🛑 STOP CONDITIONS (MANDATORY)

You MUST STOP and ASK the user if:
- An element does not clearly belong to a component
- A screen requires direct DOM access
- A folder is missing or unclear
- You are tempted to refactor or optimize structure
- The screenshot and instructions conflict

🚫 DO NOT GUESS  
🚫 DO NOT ASSUME  
🚫 DO NOT CREATE WORKAROUNDS  

---

## 🎯 GOAL

Produce code that is:
- Predictable
- Scalable
- Strictly layered
- Fully compliant with the existing structure
- Free of architectural violations

**Correct structure > working code.**
